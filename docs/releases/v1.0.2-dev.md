# cfx-mongodb v1.0.2-dev — FiveM FS sandbox compat

> **Channel:** `dev` (unstable pre-release)  
> **Base version:** `1.0.2`  
> **Previous dev:** [`v1.0.1-dev`](v1.0.1-dev.md) · **Stable baseline:** `v1.0.0` on `main`

Dev pre-release that fixes MongoDB connect under FiveM’s Node FS sandbox (`.dockerenv` probe).

---

## Highlights

| Area | What's new in 1.0.2 |
|------|---------------------|
| **Runtime** | `src/fivemFsCompat.ts` stubs `fs.promises.access` for `/.dockerenv` → ENOENT (official [Cfx workaround](https://docs.fivem.net/docs/scripting-manual/migrating-from-other-platforms/)) |
| **Tests** | **137** unit tests (incl. `fivemFsCompat`) |
| **Docs** | Troubleshooting for the sandbox error; live-test note for txData checkouts |

---

## What is included (runtime)

Same export API as v1.0.1 — no new `server_exports`. See [API.md](https://github.com/RaP-SoR/cfx-mongodb/blob/dev/docs/API.md).

| Area | Details |
|------|---------|
| **Runtime** | FiveM Node **22** only (`node_version '22'`, `mongodb@7`) |
| **Compat** | FS patch loads from `index.ts` **before** bootstrap / `MongoClient` |
| **Contract** | `{ success, ... }` envelopes — never throws; `insertedId` always string |
| **Tests** | 137 unit tests + integration tests (CI) |

---

## Install (from this ZIP)

1. Extract to `resources/cfx-mongodb` (folder name must match).
2. Set MongoDB ConVars — see [CONFIGURATION.md](https://github.com/RaP-SoR/cfx-mongodb/blob/dev/docs/CONFIGURATION.md).
3. In `server.cfg`: `ensure cfx-mongodb` **before** consumer resources.
4. Wait for `cfx-mongodb:ready` before calling exports.
5. Read `BUILD_INFO.txt` inside the ZIP for branch, commit, and channel.

**No local `yarn build` required** — `dist/` and production `node_modules/mongodb` are bundled.

---

## Optional: npm types package (same Release)

Each Release also ships **`cfx-mongodb-v1.0.2-dev.tgz`** — TypeScript types only.

```bash
yarn add cfx-mongodb@https://github.com/RaP-SoR/cfx-mongodb/releases/download/v1.0.2-dev/cfx-mongodb-v1.0.2-dev.tgz
```

```typescript
import type { CfxMongoInsertResult } from "cfx-mongodb/types";
```

Pin the same tag as the server ZIP.

---

## Upgrade from v1.0.1-dev

- Drop-in replacement — same export signatures
- Fixes connect failure: `Access to this API has been restricted` on `/.dockerenv` / `C:\.dockerenv`
- After install: restart the resource/server; confirm `getVersion()` is `1.0.2-dev`

Full list: [CHANGELOG.md — 1.0.2](https://github.com/RaP-SoR/cfx-mongodb/blob/dev/docs/CHANGELOG.md#102--fivem-fs-sandbox-compat-2026-07-24)

---

## Version string at runtime

```lua
local v = exports['cfx-mongodb']:getVersion()
-- dev git checkout: "1.0.2-dev"
-- dev ZIP (push artifact): "1.0.2-dev+dev.abc1234"
-- dev tag v1.0.2-dev: "1.0.2-dev"
-- stable (main): "1.0.2"
```

Any **`-dev`** in the string = unstable — not for production.

---

## Path to stable v1.0.2

1. Continue on **`dev`** → pre-releases `v1.0.2-dev`, …
2. Manual practice test + bug-fix tour ([PRE-STABLE-FIXLIST.md](https://github.com/RaP-SoR/cfx-mongodb/blob/dev/docs/plans/PRE-STABLE-FIXLIST.md))
3. Merge **`dev` → `main`**
4. Tag **`v1.0.2`** on `main` → stable GitHub Release (no `-dev` in fxmanifest)

---

## Links

- [Changelog 1.0.2](https://github.com/RaP-SoR/cfx-mongodb/blob/dev/docs/CHANGELOG.md)
- [CONFIGURATION troubleshooting](https://github.com/RaP-SoR/cfx-mongodb/blob/dev/docs/CONFIGURATION.md#troubleshooting-config)
- [Security](https://github.com/RaP-SoR/cfx-mongodb/blob/dev/docs/SECURITY.md)
